{
  "name": "exercises-dataset",
  "version": "1.1.0",
  "description": "Capacitor packaging for the Exercises Dataset interactive browser as an offline Android app.",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "www": "node scripts/prepare-www.mjs",
    "sync": "npm run www && cap sync android",
    "apk": "npm run sync && cd android && gradlew assembleDebug && cd .. && node scripts/dist.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.0.0",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0",
    "@capacitor/toast": "^8.0.0"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0"
  }
}
